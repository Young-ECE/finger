{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "⚡️ 终极一键调试 (Task Based)",
            "type": "cortex-debug",
            "request": "attach",      // 1. 使用吸附模式，永远不会报 Timeout
            "servertype": "external", // 2. 告诉插件：OpenOCD 已经在外面跑了（其实是我们的 Task 跑的）
            
            "gdbTarget": "localhost:3333", // 3. 直接连 3333，稳如老狗
            "gdbPath": "/usr/bin/gdb-multiarch",
            
            "executable": "${workspaceFolder}/build/finger.elf",
            "cwd": "${workspaceFolder}",
            
            // 4. 【核心黑科技】启动调试前，先自动运行我们定义的 OpenOCD 任务
            "preLaunchTask": "🚀 Build & Start OpenOCD (Final)",
            
            "runToEntryPoint": "main",
            "svdFile": "${workspaceFolder}/STM32F446x.svd",
            "showDevDebugOutput": "raw"
        }
    ]
}